<% if current_user %>
  <script>
    window.chatBoomSettings = {
      email: "<%= current_user.email %>", // user.email
      created_at: <%= current_user.created_at.to_i %>, // user.created_at.to_i(unix timestamp)
      app_id: "26272e79-bd5c-4a2b-bb6f-cb3dc9bcf8a1"
    };
  </script>
  <script>(function () {
    var w = window;
    var ic = w.ChatBoom;
    if (typeof ic === "function") {
      ic('reattach_activator');
      ic('update', chatBoomSettings);
    } else {
      var d = document;
      var i = function () {
        console.log("arguments", arguments);
        i.c(arguments)
        debugger
      };
      i.q = [];
      i.c = function (args) {
        console.log("args", args);
        i.q.push(args)
        debugger
      };
      w.ChatBoom = i;
      function l() {
        var s = d.createElement('script');
        s.type = 'text/javascript';
        s.async = true;
        s.src = 'http://chatboom.herokuapp.com/widget/26272e79-bd5c-4a2b-bb6f-cb3dc9bcf8a1';
        var x = d.getElementsByTagName('script')[0];
        x.parentNode.insertBefore(s, x);
      }

      if (w.attachEvent) {
        w.attachEvent('onload', l);
      } else {
        w.addEventListener('load', l, false);
      }
    }
  })()</script>

<% end %>