<div class="col-xs-12">
  <h2>Dashboard <small><%= @site.name %><i>(<%= link_to "update", edit_site_path(@site) %>)</i></small></h2>

  Events: <%= @events.count %>
  <hr/>
  <% @events.each do |event| %>
    <%= event.inspect %>
    <hr/>
  <% end %>
</div>
